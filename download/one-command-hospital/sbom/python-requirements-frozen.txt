# Python services — frozen dependency baseline (v0.4.0)
#
# Union of services/*/requirements.txt, pinned. This file is the SBOM input;
# CI regenerates/verifies it (pip freeze comparison) and the release process
# feeds it to an SBOM generator (syft/cyclonedx) — see sbom/README notes in
# CONTRIBUTING.md. Keep honest: pins here are what the Dockerfiles install
# as floors; CI flags any drift.

# ── shared by all FastAPI services ──
fastapi==0.115.*
uvicorn==0.34.*
pydantic==2.10.*
requests==2.32.*
PyYAML==6.0.*

# ── guideline-rag ──
rank-bm25==0.2.2
numpy==2.2.*
# optional vector channel (build-arg WITH_SBERT=true):
# sentence-transformers==3.*

# ── deid-gate ──
presidio-analyzer==2.2.*
presidio-anonymizer==2.2.*
spacy==3.8.*
# model: en_core_web_sm (MIT-adjacent CC BY-SA 3.0 — see license register)

# ── verifier ──
medspacy==1.3.*

# ── eval + loadtest ──
# (dev-scope; not shipped in runtime containers)
pytest==8.*
httpx==0.28.*
locust==2.*
